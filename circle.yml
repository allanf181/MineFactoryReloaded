machine:
  java:
    version: openjdk8
    
dependencies:
  override:
    - git submodule update --init --recursive
    - chmod +x gradlew
    - ./gradlew setupCIWorkspace
    
compile:
  override:
    - ./gradlew build
    - mv ./build/libs/* $CIRCLE_ARTIFACTS
